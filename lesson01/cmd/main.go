package main

import (
	// "context"
	"fmt"
	// "os"
	// "os/signal"

	// "github.com/ptsypyshev/gb-golang-backend-level02/lesson01/internal/mappers"
	// "github.com/ptsypyshev/gb-golang-backend-level02/lesson01/internal/models"
)

func main()  {
	// ctx, cancel := signal.NotifyContext(context.Background(), os.Interrupt)
	// users := mappers.NewUserMapper(someStore)
	// groups := mappers.NewGroupMapper(someStore)
	// newUser, err := users.Create(ctx, models.User{})
	// newGroup, err := groups.Create(ctx, models.Group{})
	
	fmt.Println("It's ready! But storage and methods is not implemented now :-(")
}